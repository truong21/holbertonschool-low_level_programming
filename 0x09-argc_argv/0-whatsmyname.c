#include "holberton.h"
#include <stdio.h>
/**
 * main - prints the program name
 * @argc: argument count
 * @argv: arguments calling function
 * Return: 0 (Success)
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);

	return (0);
}
